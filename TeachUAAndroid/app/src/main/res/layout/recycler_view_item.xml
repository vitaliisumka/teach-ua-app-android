<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="380dp"
    android:layout_height="465dp"
    android:layout_gravity="center"
    android:layout_marginTop="24dp"
    android:layout_marginBottom="30dp"
    android:elevation="5dp"
    app:cardBackgroundColor="@color/black"
    app:cardCornerRadius="30dp"
    app:cardElevation="4dp"
    app:strokeColor="@color/white"
    app:strokeWidth="12dp">



    <ImageView
        android:id="@+id/imageInMainScreenCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:alpha="0.9"
        android:scaleType="centerCrop" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@+id/buttonInMainScreenCardMoreInfo"
            android:layout_marginBottom="15dp">

            <ImageView
                android:id="@+id/container"
                android:layout_width="match_parent"
                android:layout_height="140dp"
                android:layout_centerInParent="true"
                android:alpha="0.8"
                android:background="@drawable/rectangle3" />

            <TextView
                android:id="@+id/textInMainScreenCard"
                android:layout_width="290dp"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_marginTop="30dp"
                android:gravity="center"
                android:text="Text Text Text Text TextText TextText Text Text Text Text Text Text Text TextText"
                android:textColor="@color/white"
                android:textSize="19sp" />
        </RelativeLayout>

        <Button
            android:id="@+id/buttonInMainScreenCardMoreInfo"
            android:layout_width="190dp"
            android:layout_height="45dp"
            android:layout_alignParentBottom="true"
            android:layout_centerInParent="true"
            android:layout_marginBottom="30dp"
            android:background="@drawable/rounded_button_in_card"
            android:gravity="center"
            android:paddingBottom="6dp"
            android:text="Детальніше"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textSize="17sp"
            app:backgroundTint="@color/firstFragmentButtonColorMoreInfo" />

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>
